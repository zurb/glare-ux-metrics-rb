# frozen_string_literal: true

RSpec.describe Glare::Ux::Metrics::Rb do
  it "has a version number" do
    expect(Glare::Ux::Metrics::Rb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
